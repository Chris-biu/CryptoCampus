{
  "name": "cryptocampus-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test && playwright test --config playwright.runtime.config.ts",
    "test:runtime": "playwright test --config playwright.runtime.config.ts",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
